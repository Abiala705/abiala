import Particles from "react-tsparticles";
import particlesConfig from "./particles.config";

const ParticlesBackgroundc = () => {
  return <Particles params={particlesConfig} />;
};

export default ParticlesBackgroundc;
